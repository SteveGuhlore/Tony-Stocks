# Session Handoff — 2026-06-02

Paste this into the new session for full context.

## TL;DR
- The **dashboard overhaul ("Tony's cockpit") is DONE and merged to `main`** (16 commits, 40 vitest tests, clean build).
- Next job: **build the Alpaca paper-trading execution subsystem** per `docs/superpowers/specs/2026-06-02-paper-trading-design.md`.
- Coordinate the **Command Center bridge** with the other terminal per `docs/CONTRACTS/command-center-bridge.md`.

## Repo / how to run
- Backend API (FastAPI) serves the dashboard data; frontend expects it at **`http://localhost:8001`** (`dashboard-web/.env.local` → `NEXT_PUBLIC_API_URL=http://localhost:8001`).
- Frontend: `cd dashboard-web && npm run dev` (Next 16 + Turbopack; lands on :3000/3001). **This is a modified Next.js — read `dashboard-web/node_modules/next/dist/docs/` before editing app code (per `dashboard-web/AGENTS.md`).**
- Frontend tests: `cd dashboard-web && npm test` (Vitest; pure-logic libs under `lib/*.test.ts`). 40 passing.
- Backend tests: `powershell -ExecutionPolicy Bypass -File .\scripts\run_tests.ps1` (pytest, `$env:PYTHONPATH="src"`).
- Demo mode (no Alpaca keys) runs safe; `live_trading_enabled: false` in `config/default_config.yaml`.

## What the dashboard overhaul shipped (so the new session doesn't redo it)
Final IA: **`/` The Board** + **`/record` Track Record** only (7 old pages deleted; old `Sidebar.tsx` gone).
- `dashboard-web/lib/`: `format.ts`, `plan.ts` (railPositionPct), `signal.ts` (verdictDisplay/statusKind/Tone), `board.ts` (boardStatus/nearEntry/isClosedOutcome), `chart.ts` (priceBounds/scaleY/priceTicks) — all unit-tested.
- `components/board/`: `cells.tsx` (PlanRail/VerdictChip/ScorePair/TONE_VAR), `BoardTable.tsx` (Watches), `UniverseTable.tsx` (scan), `PlanChart.tsx` (candles+volume+axes+crosshair+plan lines).
- `components/layout/StatusBar.tsx` (ambient rail), `components/overlays/SymbolDrawer.tsx` (the DeepDive).
- `app/page.tsx` (Board + Watches/Universe toggle), `app/record/page.tsx` (Track Record).
- Tokens in `app/globals.css` (azure/brass/muted Tone classes + `--azure`).

## Command Center bridge — the bot's side (reconcile with the other terminal)
Two independent agents meet at a JSON contract (`docs/CONTRACTS/command-center-bridge.md`).

**What the trading bot (THIS repo) PROVIDES to the Command Center:**
- Tony's picks — produced by `src/trading_bot/vault/bridge.py` and `src/trading_bot/agent_bridge.py` (markdown bridge today). Per pick: `symbol`, Tony `score`, `entry`/`stop`/`target`, thesis/`tony_hypothesis`, setup_category, and a dedup key (`symbol` + `scan_run_id`, or `symbol`+date).
- The other terminal's `tony_bridge.py` is the **consumer** that ingests this.

**What the trading bot NEEDS BACK from the Command Center (to fill the dashboard's second layer):**
- A new endpoint **`GET /api/command-center`** returning `CommandCenterResponse` (see contract): `picks` (per-symbol `score`, `verdict` enum reaffirm/adjust/override/close, `reasoning`, `returned_at`), `record` (win_rate/avg_pl/target_hits/stop_hits/equity_curve), `agreement` tallies.
- Until that endpoint exists, the dashboard's `useCommandCenter()` hook returns empty and every second-layer slot shows "⋯ awaiting" — no errors. Frontend types already defined in `dashboard-web/lib/types.ts` (`CommandCenter*`).
- **Verdict enum and 0–100 score scale MUST match** `dashboard-web/lib/signal.ts`. `equity_curve` must share Tony's basis so the two lines compare.

**Action for new session:** once the other terminal reports its bridge format, (a) confirm it matches the contract, (b) add the `/api/command-center` FastAPI route that reads the Command Center output and serves `CommandCenterResponse`.

## Next subsystem: Alpaca paper trading (the main task)
Spec: `docs/superpowers/specs/2026-06-02-paper-trading-design.md`. Decisions locked: **real Alpaca paper account** + **auto-submit on `entry_triggered`**, behind fail-closed safety gates.
- Existing scaffolding: `src/trading_bot/execution/paper.py` (in-memory `PaperBroker` stub), `src/trading_bot/paper/paper_journal.py` (P&L helper only). Master flag `live_trading_enabled: false` (keep false; paper uses a **separate** `paper_trading.enabled` flag).
- 6 phases in the spec: config+flags → pure order-router (TDD: sizing + gates) → `AlpacaPaperBroker` (alpaca-py, paper=True, + FakeBroker for tests) → storage/journal → watch-loop wiring + reconciliation → API + dashboard surface.
- Start by brainstorming/confirming the open questions in the spec (sizing basis, DAY vs GTC, whether to gate on Command Center verdict).
- Recommended: TDD the order router first (pure, no network), then the broker behind a fake.

## Gotchas in this environment
- A GateGuard hook interrupts Write/Edit/destructive Bash asking to "present facts" — answer briefly (file, purpose, no prod data, the instruction) and retry. To silence it for a session: `ECC_GATEGUARD=off` or add `pre:edit-write:gateguard-fact-force` to `ECC_DISABLED_HOOKS`.
- A cost-tracking hook prints stale/critical cost notices — informational.
- Windows: CRLF/LF git warnings are harmless. Use PowerShell or the Bash tool.
- Don't kill all `node` processes — the Command Center terminal and MCP servers run node too.

## State of git
- `main` has the merged overhaul + both specs + the contract.
- Branch `feat/dashboard-overhaul` can be deleted (already merged).
- Nothing pushed beyond `origin/main`'s prior state unless you push.
