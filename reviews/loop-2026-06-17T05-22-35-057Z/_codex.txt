{"verdicts":[{"id":0,"verdict":"valid","reason":"The safety baseline hashes only the startup `reports/*.json` set, then post-checks only that set plus CC bridge child names and a vault item count, so new report files and in-place bridge/vault content edits can slip through."},{"id":1,"verdict":"valid","reason":"Like the other harness, it fingerprints only the startup bot report set and compares the bot vault by count only, so new `reports/*.json` files and real vault content edits are not reliably detected."},{"id":2,"verdict":"valid","reason":"Both writers do unlocked read-modify-write on the same JSON file, and `_load_all()`/the batch loader treat decode errors as empty data, so concurrent writes can lose or wipe history."},{"id":3,"verdict":"valid","reason":"`win_rate()` counts all target/stop outcomes in `conclusive_trades()`, but `_pl_from_conclusive()` drops invalid long brackets (`target<=entry` or `stop>=entry`), so summary metrics can be computed from different trade sets."},{"id":4,"verdict":"valid","reason":"`build_evaluated_picks()` joins signals only by uppercased symbol with no pick-date key, so repeated picks in one ticker reuse one signal bundle instead of pick-time data."},{"id":5,"verdict":"valid","reason":"The module executes provider construction, live API calls, printing, and `sys.exit()` at top level, so merely importing it can hit the network and terminate the importer."},{"id":6,"verdict":"valid","reason":"`days_active_map` is computed once over the full snapshot window and then reused for each historical `date_str`, so older backfilled notes get future-aware activity counts."},{"id":7,"verdict":"invalid","reason":"The code intentionally emits separate alternative calibration proposals, and the activation path explicitly requires choosing one proposal when multiple are approved, so each `new_weights` being rooted in `current_weights` is by design."},{"id":8,"verdict":"valid","reason":"`_funnel_eval_signals_from_snapshots()` sorts ascending and stores `grp.iloc[0]` per symbol, so every repeated pick in that ticker is evaluated against the oldest snapshot in the window, not a pick-time snapshot."},{"id":9,"verdict":"valid","reason":"The script imports `trading_bot.*` before adding `src/` to `sys.path`, so `python scripts/verify_research_stack.py` from the repo root fails unless the package is already installed or `PYTHONPATH` is set."},{"id":10,"verdict":"valid","reason":"It unconditionally writes `vault/HOME.md` and each top-level `<dir>/_index.md`, so any pre-existing hand-authored files with those names are overwritten despite the non-destructive claim."}],"new":[{"severity":"low","file":"scripts/index_cc_vault.py","issue":"The script claims to generate `_index.md` for every directory, but it only writes indexes for top-level directories and never creates `_index.md` files for nested folders.","fix":"Walk directories recursively and emit an `_index.md` inside each directory based on that directory's direct files/subdirectories."},{"severity":"low","file":"src/trading_bot/analytics/funnel_eval.py","issue":"`build_evaluated_picks()` stores `record['return_pct']` in the `r_multiple` field, so `kept_avg_r`/`dropped_avg_r`/`avg_r_delta` are labeled as R-multiples while actually reporting percentage returns.","fix":"Either rename the fields/output to return-percentage terms or compute true R-multiples from stored entry/stop/exit data before averaging."}]}