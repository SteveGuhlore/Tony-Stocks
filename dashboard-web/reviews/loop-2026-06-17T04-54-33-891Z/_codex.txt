[{"severity":"high","file":"components/views/SystemView.tsx","issue":"All four system controls are permanently inert: each confirmation spec passes `onConfirm: () => {}`, so confirming 'Stop watch loop', 'Pause paper trading', 'Flatten all positions', or 'Trigger scan now' performs no action while the UI implies the control succeeded.","fix":"Replace each empty `onConfirm` with the real mutation/API call, or disable/hide the control until the backend action exists."},{"severity":"medium","file":"components/views/PaperBookView.tsx","issue":"The paper-book safety actions are also nonfunctional: both 'Flatten all' and 'Pause paper' confirmations use `onConfirm: () => {}`, so the dialog closes without changing paper-trading state.","fix":"Wire each confirmation to the corresponding paper-trading action or render the buttons disabled/unavailable until implemented."},{"severity":"medium","file":"components/kinetic/ConfirmDialog.tsx","issue":"`requirePin` does not validate a PIN; it only checks `pin.trim().length >= 4`, so any four characters authorize 'dangerous money-adjacent actions' and the PIN gate provides no real protection.","fix":"Validate the entered PIN against an expected value or rename this to a typed-confirmation prompt instead of a PIN check."},{"severity":"medium","file":"components/drawer/SymbolChart.tsx","issue":"If `lightweight-charts` fails to import or initialize, the `catch` block swallows the error but the render path still shows only the empty host `<div>`, producing a blank chart area instead of the documented empty state.","fix":"Store chart-init failure in component state and render `EmptyChart` when initialization fails."},{"severity":"medium","file":"components/kinetic/MiniLine.tsx","issue":"The empty-state guard is wrong for multi-series input: it checks `all.length < 2`, so two 1-point series bypass the guard, but each series is then skipped by `if (s.points.length < 2) return null`, leaving a blank chart with only legends.","fix":"Consider the chart empty unless at least one series has 2 or more points before rendering the SVG."},{"severity":"low","file":"components/kinetic/CommandPalette.tsx","issue":"Keyboard navigation can enter an invalid state when there are no results: pressing ArrowDown sets `active` to `-1` via `Math.min(a + 1, results.length - 1)`, and the reset effect does not repair negative values.","fix":"Skip ArrowUp/ArrowDown handling when `results.length === 0` and clamp `active` to the range `0..results.length-1`."}]