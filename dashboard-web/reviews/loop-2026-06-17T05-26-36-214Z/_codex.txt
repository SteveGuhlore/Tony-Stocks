{"verdicts":[{"id":0,"verdict":"valid","reason":"`useEventStream` calls `invalidateQueries([\"cockpit\"])` on every SSE message, which forces active-query refetches on top of the global 8s polling loop."},{"id":1,"verdict":"valid","reason":"`seedTicks()` never overwrites existing map entries, and `TapeRow` prefers `tick.price/changePct` over fresh polled row data, so stale live ticks can mask newer snapshots indefinitely."},{"id":2,"verdict":"valid","reason":"`Tape` filters only on `row.status`, while `statusBadge()`/`StatusCell` also treat `entry_triggered` as triggered, so visibly triggered rows can be excluded from the Triggered filter."},{"id":3,"verdict":"valid","reason":"`Cockpit` stores only the selected symbol, but `SymbolDrawer` opens only when `rows.find(...)` succeeds, so prep/paper symbols absent from `data.rows` silently do nothing."},{"id":4,"verdict":"valid","reason":"`SymbolChart` swallows import/init failures in `catch` without setting any fallback state, leaving the rendered chart host empty instead of showing an error placeholder."},{"id":5,"verdict":"valid","reason":"`autoPhase()` falls back to ET hour only, so 09:00-09:29 ET and weekends/holidays during market hours are incorrectly classified as `live` when metadata is missing."},{"id":6,"verdict":"valid","reason":"The command-palette confirm specs in `app/Cockpit.tsx` use empty `onConfirm` handlers, so those actions always no-op unlike the wired controls elsewhere."},{"id":7,"verdict":"valid","reason":"`changeClass()` returns `\"pos\"` for `v >= 0`, so zero changes and flat P/L are styled as gains across consumers."}],"new":[{"severity":"low","file":"lib/api.ts","issue":"The top-bar `paper equity` KPI can never show a value because `api.paper()` hardcodes `equity: null`, and `Cockpit` passes that straight through to `TopBar`.","fix":"Populate `PaperResponse.equity` from a backend field or derive it from the latest `/api/paper/equity-curve` point before rendering the header."},{"severity":"low","file":"lib/priceStore.ts","issue":"The global `ticks` map is never pruned, so symbols that fall out of the cockpit universe accumulate for the lifetime of the session.","fix":"Prune `ticks` against the current symbol set during snapshot seeding or add an explicit cleanup path when the universe changes."}]}